## Packages
recharts | Data visualization for student academic view

## Notes
- Tailwind Config: Need to add custom font families for `Outfit` (display) and `Inter` (sans).
- The `recharts` package is used for displaying attendance and performance charts on the dashboards.
- Authentication uses a mock endpoint `/api/login` and `/api/user` expected to be handled by the backend.
- External links are mocked to open in new tabs as requested.
